import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(({ palette }) => ({
  drawer: {},
  list: {
    width: 270,
    padding: 8,
  },
  menuItem: {
    marginBottom: 8,
  },
}));
